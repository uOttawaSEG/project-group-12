<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Group12OTAMS">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".TutorDashboardActivity" />
        <activity android:name=".CreateAvailabilityActivity" />
        <activity android:name=".PastSessionsActivity" />
        <activity android:name=".PendingRequestsActivity" />
        <activity android:name=".UpcomingSessionsActivity" />
        <activity android:name=".TeacherRegistrationActivity" />
        <activity android:name=".StudentRegistrationActivity" />
        <activity android:name=".WelcomeActivity" />
        <activity android:name=".AdminApprovalActivity" />
        <activity android:name=".StudentSessionsActivity" />
        <activity android:name=".StudentSearchActivity" />
    </application>

</manifest>